## 🚀 Запуск приложения

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/HappySaber/PR_handller.git
   ```
2. Переместитесь в рабочий репозиторий:
   ```bash
   cd PR_handller
   ```
3. Измените .env.exemple на .env и заполните поля своими данными:
4. Запуск приложения из его корня:
 ```bash
docker-compose up --build
```
5. После сборки api сервиса будет доступен по адресу:
```bash
http://localhost:8080/
```

Для запуска линтера команда из корня приложения:
```bash
golangci-lint run 
```
Если не скачен данный линтр, можно скачать командой на Windows(нужно зайти в PowerShell от имени администратора, если установлен choco), также можно установить через GitHub за инструкцией в интернет:
```bash
choco install golangci-lint -y
```

### Использование приложения
Для тестов предусмотрены 2 эндпоинта который добавит тестовых пользователей, а другой удалит, если в .env ENV=local.
### Реализация дополнительных заданий
Были реализованы данные задания
1. Описать конфигурацию линтера.
2. Добавить простой эндпоинт статистики (например, количество назначений по пользователям и/или по PR)

### Вопросы по заданиям
1. Должно ли было обрабатывать "ошибка", если на PR отправлялось задание, но при этом нет людей в команде с is_active=true
2. Задание (Добавить метод массовой деактивации пользователей команды и безопасную переназначаемость открытых PR (стремиться уложиться в 100 мс для средних объёмов данных).)
Что должно происходить с пул реквестами этой команды, когда вся она деактивируется? Ревьюверы должны удаляться из пулреквестов или что-то другое?
3. В вашем openapi.yaml присутствуют         - AdminToken: [] и  - UserToken: [] должно ли это было как-то реализовываться?

## 📡 Endpoints (REST API)
---

### Endpoints
| Метод | Endpoint                  | Описание                         
|-------|----------------------------|----------------------------------
| POST  | `/test/add`              | Создать тестовых пользователей   
| DELETE  | `/test/delete`             | Удалить тестовых пользователей     
| POST  | `/team/add`    | Создать новую команду               
| GET   | `/team/get`          | Получить список участников команды             
| GET   | `/users/getReview` | Получить PR, в которых пользователь назначен ревьювером  
| PUT  | `/users/setIsActive`  | Активировать/деактивировать пользователя 
| POST  | `/pullRequest/create`    |  Создать новый Pull Request            
| PUT   | `/pullRequest/merge`          |  Смержить Pull Request         
| PUT   | `/pullRequest/reassign` | Переназначить ревьювера на Pull Request
| GET  | `/stats/reviews`  |  Получить статистику назначений PR по пользователям 

---
## 🏗 Используемый стек

- **Go (Gin, Goose)** – backend
- **PostgreSQL** – основная база данных
- **Docker & Docker Compose** – контейнеризация и запуск приложения
